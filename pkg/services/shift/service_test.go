package shift_test

import (
	"testing"
)

func TestShiftService(t *testing.T) {
	t.Parallel()

	//logger := utils.NewMockLogger()

	t.Run("reject saving shifts. duplicate in db", func(t *testing.T) {
		t.Parallel()

	})

	t.Run("save shifts", func(t *testing.T) {
		t.Parallel()

	})
}
